Use the `quest-readme` skill to provide a brief, human-readable summary of the Quest framework.

Arguments: $ARGUMENTS (optional focus such as `lifecycle`, `commands`, `files`, `new user`, `manual`, or `full`)

Default to a concise summary when no arguments are provided. Use longer manual-style output only when the arguments request it.
